# Minecraft Resource Hub

Đã cập nhật:
- Thư mục images/ hỗ trợ lưu ảnh cục bộ.
- Trình bày chi tiết thông số máy farm Litematica theo từng dòng trong file `data.js`.
- Bổ sung `white-space: pre-line` trong `style.css` để nhận diện ký tự xuống dòng.
